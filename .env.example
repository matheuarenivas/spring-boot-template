# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=templatedb
DB_USERNAME=appuser
DB_PASSWORD=changeme

# OIDC / JWT — issuer URI for token validation
OIDC_ISSUER_URI=https://accounts.google.com

# CORS — comma-separated allowed origins (optional, defaults to localhost)
# APP_CORS_ALLOWED_ORIGINS=https://yourdomain.com
